type User {
  id: ID! @id
  nick: String!
}

type Round {
  word1: String!
  word2: String!
}

type Lobby {
  id: ID! @id
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  started: Boolean!
  creator: User!
  partner: User
  creator_word: String
  partner_word: String
  rounds: [Round!]
}